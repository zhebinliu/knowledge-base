<?xml version="1.0" encoding="UTF-8"?>
<ObjectLayout xmlns="http://sharecrm.com/metadata">
    <content>{{CONTENT_JSON}}</content>

    <!-- 状态: new | modified | unchanged -->
    <status>{{STATUS}}</status>
</ObjectLayout>

<!--
使用说明：
1. 将 {{CONTENT_JSON}} 替换为完整的布局 JSON 字符串（转义后的单行 JSON）
2. 将 {{STATUS}} 替换为状态值：new（新建）、modified（修改）、unchanged（未改动）
3. 文件命名：<layoutApiName>.layout-meta.xml
4. 保存路径：tenant-config/objects/<ObjectApiName>/layouts/

content JSON 中必须包含的顶层字段：
- api_name: 布局 API Name
- display_name: 显示名称
- layout_type: detail | edit | list_layout | list
- is_default: 是否默认布局
- ref_object_api_name: 所属对象 API Name
- components: 组件列表
- layout_structure: 页面排布结构

详细规格见 ../references/layout-spec.md
-->
