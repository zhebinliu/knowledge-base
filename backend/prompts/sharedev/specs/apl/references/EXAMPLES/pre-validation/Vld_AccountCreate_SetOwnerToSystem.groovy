/**
 * @author Claw
 * @codeName 客户 - 新建前验证 - 负责人默认设为系统
 * @description 客户对象 AccountObj 新建前，将负责人 (owner) 字段赋值为系统用户 (-10000)
 * @createTime 2026-03-12
 */

// ============================================
// Step 1: 获取 context 传入的数据
// ============================================

// ✅ 按钮/前验证场景：用 context.data 获取记录数据
Map dataMap = context.data as Map;

// ============================================
// Step 2: 设置负责人为系统用户
// ============================================

// ⚠️ 注意：owner 字段类型是 employee，值应该是员工 ID 字符串
dataMap.put("owner", "-10000"); // 系统用户 ID

log.info("客户新建前验证：负责人已设置为系统用户 (-10000)");

// ============================================
// Step 3: 返回验证结果
// ============================================

// ✅ 返回 success：error=false, block=false
return ["error": false, "errorMessage": "成功"];
