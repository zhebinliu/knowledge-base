/**
 * ProjectTodos (Redesign) — 复用旧版组件,传 variant='redesign' 走深色 Liquid Glass 主题
 */
import ProjectTodos from '../../pages/console/ProjectTodos'

export default function ProjectTodosRedesign() {
  return <ProjectTodos variant="redesign" />
}
